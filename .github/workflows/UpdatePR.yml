name: Update PR on main repo w/ latest VSCode build
on: workflow_dispatch

jobs:
  foo:
    runs-on: ubuntu-latest
    env:
      CDK_DIR: ${{ 'bal' }}${{ 'rpo' }}
      TF_CMD: terraform${{ true && ' -chdir='env.CDK_DIR }}
    steps:
      - run: echo "${{ env.TF_CMD }}"
